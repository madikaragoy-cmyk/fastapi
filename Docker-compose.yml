version: '3.9'

services:

  fastapi-app:
    build: .
    ports:
      - "8000:8000"
    volumes:
      - .:/app
      environment:
        - PYTHONNUNBUFFERED=1

  test:
    build: .
    command: pytest